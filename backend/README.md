# Legacy backend

This folder contains an old mock FastAPI service and is not part of the live SafeRoute runtime.

Use the root `main.py` entrypoint, which imports `app.main:app`, or run `uvicorn app.main:app`.
