# Legacy saferoute-core

This folder is an experimental historical backend and is not part of the live production MVP runtime.

The active backend package is `/app`; the active browser-facing API is served from root `main.py`.
